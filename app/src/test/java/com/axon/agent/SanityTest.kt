package com.axon.agent

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test so the JVM test source set and the test runner are wired
 * from Stage 0. Real unit tests (JSON-RPC serialization, action-mapping tables,
 * bounds/center math, binary frame header) arrive with the logic in Stage 1+.
 */
class SanityTest {
    @Test
    fun toolchainWorks() {
        assertEquals(4, 2 + 2)
    }
}
